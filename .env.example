# Build-time: baked into dist/app.js by `npm run build`.
# Empty = same-origin (a reverse proxy routes /api and /readyz to the backend).
API_URL=
BUILD_VERSION=dev

# Local dev server only (`npm run serve`).
PORT=8080
