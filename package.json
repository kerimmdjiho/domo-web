{
  "name": "domo-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node --env-file-if-exists=.env scripts/build.js",
    "serve": "node --env-file-if-exists=.env scripts/serve.js",
    "clean": "node -e \"import('node:fs').then(fs=>fs.rmSync('dist',{recursive:true,force:true}))\""
  }
}
